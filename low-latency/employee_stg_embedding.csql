
select stg.name as employeename,
stg.code as employee_code,
face_embedding
--metadata-v2--
from stg_meuconector_employee stg
--timestamp-- WHERE _ingestionDatetime > SAFE.DATETIME(TIMESTAMP_MICROS(SAFE_CAST({{start_from}} AS INT64)))
