# REST API for testing BigQuery

# API main routes

Send a request to `https://{TENANT_NAME}-{APP_NAME}.apps.{qarol if QA else carol}.ai/query/get_nlp_product` for getting the records from `stg_nlp_product` table in BigQuery.